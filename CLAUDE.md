# Claude Instructions

Use [AGENTS.md](AGENTS.md) as the canonical project agent instructions.

This file exists only for Claude-compatible tooling that looks for
`CLAUDE.md`. Keep substantive workflow rules in `AGENTS.md` to avoid duplicate
context.
