---
id: eig-7734
status: closed
deps: []
links: []
created: 2026-05-14T05:55:02Z
type: task
priority: 1
assignee: lispmeister
---
# Record confirmed_already_in_state for adapter no-op evidence (§10)

Spec §10 already treats `confirmed_already_in_state` as the terminal status when the pre-command fan state was already correct, or when the adapter reports the target was already in the requested state. The remaining work is to wire that adapter no-op evidence through the after-action pipeline so runtime and trace output stay consistent. No spec change is needed.

## Acceptance Criteria

The after-action path records `confirmed_already_in_state` for adapter no-op evidence without introducing a new fault type. Golden traces and runtime tests cover the no-op path, and `mix test` stays green.
