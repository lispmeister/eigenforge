# Lab Journal - 2026-05-14b Fresh-Eyes Review: Implementation Gaps and Spec Ambiguities

## Session Goals

1. Re-read `PROTOTYPE-V1-SPEC.md` and all `eigenforge_core` source files with fresh context after the six-ticket session (`b423bc4`).
2. Identify implementation gaps, correctness issues, and code quality problems against the spec.
3. Identify spec ambiguities and inconsistencies that need resolution before V1 is considered complete.
4. Capture findings as a lab journal entry and file actionable tickets.

## Context

- Project state: V1 implementation at commit `b423bc4`. All six correctness tickets from the prior session (`eig-9e9a`, `eig-it3u`, `eig-a6zg`, `eig-nl3o`, `eig-weaz`, `eig-gm9l`) closed. 96 tests passing. Three open tickets from prior session: `eig-igam` (P1), `eig-yesq` (P2), `eig-ibj4` (P3).
- Active spec: `PROTOTYPE-V1-SPEC.md` §4, §7, §9, §10, §11.
- Relevant commits: `b423bc4` (baseline for this review).

## Work Log

| Time | Observation / Action | Evidence |
|------|----------------------|----------|
| 2026-05-14T06:00Z | Read `PROTOTYPE-V1-SPEC.md` in full, then read all `eigenforge_core` source files: `snapshot_subscriber.ex`, `ledger_sqlite.ex`, `ledger_writer.ex`, `ledger_projections.ex`, `after_action_observer.ex`, `command_issuer.ex`, `trace.ex`. | All files read; notes taken per section. |
| 2026-05-14T06:30Z | **Issue #2 found:** `Trace.build_ledger` calls a local `event_type/1` that maps `%PolicyDecision{decision: "deny_stale_snapshot"}` directly to `"stale_snapshot_denied"` (one event). `SnapshotSubscriber.append_payloads` + `append_stale_deny/3` emits two events: `policy_decision_recorded` then `stale_snapshot_denied`. Trace and runtime produce different ledger event sequences for the stale CO2 path. | `trace.ex:195`; `snapshot_subscriber.ex:190-243`. |
| 2026-05-14T06:35Z | **Issue #1 found:** `LedgerSQLite` uses parameterized bind params (`?1, ?2, ...`). `LedgerProjections` builds all SQL via string interpolation with `sql_string/1` and `sql_nullable/1` helpers. `SnapshotSubscriber` uses inline `String.replace(value, "'", "''")` for two queries. Inconsistent and contrary to the spirit of the `eig-it3u` rewrite. | `ledger_projections.ex:220-260`, `snapshot_subscriber.ex:178-186`, `snapshot_subscriber.ex:484-490`. |
| 2026-05-14T06:40Z | **Issue #3 found:** `LedgerWriter.append_once/2` makes three separate connection-per-call round-trips: `LedgerSQLite.tail`, `LedgerSQLite.append_event`, `LedgerProjections.apply_event`. The spec §11 requires a single transaction covering ledger insert and projection update. A crash after the INSERT but before `apply_event` leaves the ledger and projections diverged. | `ledger_writer.ex:123-133`; spec §11 "begin an immediate write transaction… 5. Inserts the event. 6. Updates projection tables. 7. Commits." |
| 2026-05-14T06:45Z | **Issue #4 found:** `pre_command_snapshot/2` (recovery path) reads `room_state["source_received_seq_fan"]` and `room_state["source_received_monotonic_ms_fan"]`. Neither column exists in `latest_room_control_state` DDL. Both are always `nil`, so the fan receive seq falls back to `command.snapshot_seq` (a snapshot ordinal, not an IO-local receive counter). `validate_observation_order` then compares IO receive sequences against a snapshot ordinal — incorrect ordering evidence after restart. | `snapshot_subscriber.ex:595-598`; `ledger_projections.ex:330-374` (no fan-seq columns in DDL). |
| 2026-05-14T06:52Z | **Issue #5 found:** `LedgerWriter.prepare_ledger/3` creates a genesis when the ledger is empty and skips when it is non-empty — with no hash chain verification on the existing ledger. The spec §9 says "core verifies the local ledger hash chain before processing new snapshots." The verification task exists (`mix eigenforge.ledger.verify`) but nothing calls it at startup. | `ledger_writer.ex:199-209`. |
| 2026-05-14T06:55Z | **Issue #6 found:** `LedgerProjections.apply_event/2` calls `init(db_path)` which runs two `CREATE TABLE IF NOT EXISTS` statements before every event. This is O(n) unnecessary DDL during the startup projection rebuild. | `ledger_projections.ex:44`. |
| 2026-05-14T06:58Z | **Issue #7 found:** `SnapshotSubscriber.event_type/1` uses a `cond` with no `true ->` catch-all. An unknown struct causes `CondClauseError` with no useful context. `Trace.event_type/1` uses pattern-matched function heads, which is clearer. | `snapshot_subscriber.ex:740-748`. |
| 2026-05-14T07:05Z | **Spec ambiguity A:** §10 defines `confirmed_already_in_state` as applying when "pre-command fan state already matched" OR "the adapter reports a no-op." No `fault_type` for adapter no-op exists in §6's enum. The second clause is unreachable by any current code path. | `PROTOTYPE-V1-SPEC.md §10`. |
| 2026-05-14T07:08Z | **Spec ambiguity B:** §11 lists `stale_snapshot_denied` as a durable event type but does not specify what its `payload` field must contain. The implementation reuses the `PolicyDecision` struct. A later implementation could reasonably make a different choice with no spec to arbitrate. | `PROTOTYPE-V1-SPEC.md §11`. |
| 2026-05-14T07:10Z | **Spec ambiguity C:** §9 defines `effect_epoch` as "the latest resolved actuator state observation id for the target from `source_observation_ids.fan` when known, otherwise the latest command lifecycle terminal event id for that target, otherwise `startup`." "Resolved actuator state observation id" is not defined. It is ambiguous whether this means the current snapshot's fan observation id (always present when IO reports fan state) or the `after_action_id` of the most recently closed command. | `PROTOTYPE-V1-SPEC.md §9`. |
| 2026-05-14T07:20Z | Filed tickets for all seven implementation issues and three spec ambiguities. Updated lab journal index. | `eig-19zx`, `eig-a3xy`, `eig-tixv`, `eig-rsut`, `eig-ho8d`, `eig-xygb`, `eig-iz79`, `eig-7734`, `eig-88zz`, `eig-mo7t`. |

## Decisions

| Decision | Rationale | Consequences / Follow-up |
|----------|-----------|--------------------------|
| Atomicity fix (Issue #3) is a separate ticket that declares `eig-ibj4` as a dependency. | The atomicity fix requires a persistent connection; it cannot be done without `eig-ibj4`. Merging the two inflates `eig-ibj4`'s scope and mixes concerns (connection lifecycle vs. transaction boundary). | The atomicity ticket is blocked until `eig-ibj4` lands. The window for ledger/projection divergence remains open until both are done. |
| File spec tickets for all three ambiguities. | Ambiguities in the spec become implementation disagreements in V2. Resolving them now while the V1 code is fresh is cheaper than reconciling two divergent implementations later. | Three spec-update tickets; no code changes until the spec is settled. |
| Issue #2 (trace/runtime divergence) is P1. | Golden trace fixtures are verifiably wrong for the stale CO2 path. Any trace-based regression test for the stale case would either fail or silently accept wrong output. | Needs to be fixed before any new golden trace fixtures are committed. |

## Hypothesis vs Measured Impact

Not applicable. This was a read-and-analyze session with no code changes and no test runs.

## Verification

| Check | Result | Evidence |
|-------|--------|----------|
| Confirmed stale event divergence between `Trace.event_type/1` and `SnapshotSubscriber.event_type/1` | Confirmed | `trace.ex:195` vs `snapshot_subscriber.ex:740-748` + `append_stale_deny/3` at `snapshot_subscriber.ex:226-245` |
| Confirmed `source_received_seq_fan` absent from projection DDL | Confirmed | `ledger_projections.ex:330-374` (full DDL for `latest_room_control_state` has no fan-seq column) |
| Confirmed `apply_event` calls `init` unconditionally | Confirmed | `ledger_projections.ex:44` |
| Confirmed no hash chain verification at startup | Confirmed | `ledger_writer.ex:199-209` — only genesis creation/count check |

## Open Questions / Next Steps

- Issue #2 (trace stale event divergence) is the highest-urgency correctness issue because it means committed golden trace fixtures for the stale CO2 path are wrong. Fix before committing any new traces.
- Issue #4 (missing fan-seq columns) is silent — no test currently exercises post-recovery ordering for a fan observation arriving after the subscriber restarts. Add a test as part of the fix.
- Issues #3 and #5 (non-atomic writes, no startup verification) are both gated on `eig-ibj4` being merged first. Closing `eig-ibj4` should immediately unblock both.
- The three spec ambiguity tickets should be resolved in the spec before their corresponding code paths are exercised by new features or V2 work.

---

**Signed:** lispmeister + Claude Sonnet 4.6
**Date:** 2026-05-14T07:20:00Z

**Participants & Tools:** lispmeister; Claude Sonnet 4.6 (claude-sonnet-4-6); `tk`; Elixir 1.19; `mix test` (no new tests run this session)
**Commit / Witness:** No new commit — analysis session only. Baseline: `b423bc4`.
**Related Specs / Issues:** `PROTOTYPE-V1-SPEC.md` §4, §7, §9, §10, §11; `eig-igam`; `eig-yesq`; `eig-ibj4`; tickets filed this session: `eig-19zx` (P1 trace stale divergence), `eig-a3xy` (P1 fan-seq columns), `eig-rsut` (P2 atomic writes, blocks on `eig-ibj4`), `eig-ho8d` (P2 startup verification), `eig-tixv` (P2 parameterized SQL), `eig-7734` (P2 spec §10 no-op clause), `eig-88zz` (P2 spec §11 stale payload), `eig-mo7t` (P2 spec §9 effect_epoch), `eig-xygb` (P3 redundant init), `eig-iz79` (P3 cond catch-all).
**Next journal entry:** `journal-2026-05-15.md`
