# Lab Journal - 2026-05-15 V1 Completion: Gate, Grants, Mailbox Boundary, Trace Stability

## Session Goals

1. Finish the shortest remaining V1 sequence and verify there are no open P1 tickets.
2. Stabilize the trace goldens and remove any test contamination that could make the suite nondeterministic.
3. Record the completed work in the journal and capture the final commit.

## Context

- Project state: Prototype V1 implementation reached the final completion pass after the 2026-05-14b review session. Remaining open work was all P2 cleanup or P3 V2 work.
- Active spec: `PROTOTYPE-V1-SPEC.md` and the existing V1 trace/ledger contracts.
- Relevant commit: `2b28657` (`Finish V1 and stabilize trace tests`).

## Work Log

| Time | Observation / Action | Evidence |
|------|----------------------|----------|
| 2026-05-15T13:40Z | Reviewed the remaining open queue with `tk ready` and read the open P2/P3 tickets to separate actual V1 work from post-V1 cleanup. | `tk ready`; `tk show eig-ad2p`; `tk show eig-cm6y`; `tk show eig-cx4m`; `tk show eig-ib5q`; `tk show eig-ibj4`; `tk show eig-iz79`; `tk show eig-jyj0`; `tk show eig-ln8g`; `tk show eig-mj2w`; `tk show eig-sn7r`; `tk show eig-tixv`. |
| 2026-05-15T14:00Z | Verified the trace suite locally, found that `co2_high_fan_off` / `co2_high_fan_on` fixtures had diverged from the current runtime, and regenerated the goldens from live trace output. | `mix test apps/eigenforge_core/test/eigenforge/trace_test.exs`; `mix run --no-start ... Eigenforge.Trace.run_file/1`; `test/golden_traces/co2_high_fan_off.json`; `test/golden_traces/co2_high_fan_on.json`. |
| 2026-05-15T14:05Z | Confirmed the stale trace fixture was already correct in the runtime, and then found the remaining mismatch was caused by environment contamination from a ledger-task test that did not restore `:eigenforge_core` `:hmac_secret`. | `apps/eigenforge_core/test/eigenforge/trace_test.exs`; `apps/eigenforge_core/test/eigenforge/mix_tasks/ledger_tasks_test.exs`; `apps/eigenforge_core/test/eigenforge/trace_secret_test.exs`. |
| 2026-05-15T14:10Z | Patched `ledger_tasks_test.exs` so the setup restores the application secret on exit, preventing later trace tests from inheriting the temporary ledger-task secret. | `apps/eigenforge_core/test/eigenforge/mix_tasks/ledger_tasks_test.exs`. |
| 2026-05-15T14:15Z | Ran the full suite after the leak fix; all apps passed, including the trace suite and the IO/dashboard/mailbox suites. | `mix test` output: `eigenforge_contracts`, `eigenforge_mailbox`, `eigenforge_core`, `eigenforge_dashboard`, `eigenforge_io` all green. |
| 2026-05-15T14:20Z | Committed the V1 completion state and verified the queue now contains only P2/P3 work. | Commit `2b28657`; `tk ready`. |

## Decisions

| Decision | Rationale | Consequences / Follow-up |
|----------|-----------|--------------------------|
| Treat the remaining open tickets as post-V1 cleanup once the P1 set was gone. | The objective for this session was to finish the shortest V1 path, not to start the V2-prep queue. | V1 can be considered complete; the remaining work is P2/P3 only. |
| Regenerate the committed trace goldens from the actual test runtime, not from an ad hoc default Mix environment. | The full suite exposed a runtime mismatch that only appeared under the test environment. | `co2_high_fan_off` and `co2_high_fan_on` now match the test runtime; `co2_stale_fan_off` remained valid. |
| Restore global application state in `ledger_tasks_test.exs`. | The test temporarily changed `:eigenforge_core` `:hmac_secret` and was leaking that value into later trace tests. | The full suite is now stable and deterministic across runs. |

## Hypothesis vs Measured Impact

| Change | Hypothesis (before run) | Measurement (after run) | Evidence / Notes |
|--------|--------------------------|--------------------------|------------------|
| Regenerated trace goldens under the test runtime | The live trace output and the committed fixtures should match once both are generated from the same environment. | Confirmed after the `MIX_ENV=test` regeneration and re-run. | `mix test apps/eigenforge_core/test/eigenforge/trace_test.exs` passed after the update. |
| Restored `:eigenforge_core` `:hmac_secret` after ledger-task tests | If the secret leak is the cause, restoring it should remove the trace signature drift in the full suite. | Confirmed. The full `mix test` run completed with all apps green. | `apps/eigenforge_core/test/eigenforge/mix_tasks/ledger_tasks_test.exs`; `mix test`. |
| Final V1 audit against `tk ready` | If V1 is complete, there should be no open P1 tickets left. | Confirmed. `tk ready` shows only P2 and P3 tickets. | Ticket queue after the final run. |

## Verification

| Check | Result | Evidence |
|-------|--------|----------|
| Trace suite | Passed | `mix test apps/eigenforge_core/test/eigenforge/trace_test.exs` |
| Full suite | Passed | `mix test` |
| V1 priority queue | No open P1 tickets | `tk ready` |
| Final commit | Recorded | `2b28657` |

## Open Questions / Next Steps

- The remaining P2 tickets are the post-V1 cleanup set: adapter supervision, mailbox channel/projection/notifier modules, IO command executor/snapshot normalizer/fault-status split, SQLite connection supervision, SQL parameterization, and the snapshot coalescing predicate.
- V2 quorum work is still untouched and can start after the post-V1 cleanup queue is addressed or intentionally deferred.

---

**Signed:** Eigenforge contributors + Claude Sonnet 4.6  
**Date:** 2026-05-15T14:26:19Z  

**Participants & Tools:** lispmeister; Claude Sonnet 4.6 (claude-sonnet-4-6); Ticket CLI `tk`; Elixir 1.19; Erlang/OTP 28; Mix; `mix test`; `mix run --no-start`; `git commit`
**Commit / Witness:** `2b28657` `Finish V1 and stabilize trace tests`
**Related Specs / Issues:** `PROTOTYPE-V1-SPEC.md`; completed V1 tickets and trace fixtures; remaining open P2/P3 tickets in `.tickets/`
**Next journal entry:** `journal-2026-05-15b.md`
